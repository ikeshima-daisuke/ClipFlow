# BACKLOG

スコープ外に出した小タスクの置き場。`/backlog` で逐次消化する。

## 未着手

- **2回目以降の表示でプレビューが自動で開かない**（2026-09-04 発見・優先度低）
  `ShowAndActivate()` は毎回 `SearchText=""` / `FilterKind=null` / `SelectedIndex=0` に戻すが、
  2回目以降は値が変わらないため `SelectionChanged` が飛ばず、`RefreshPreview()` が呼ばれない。
  結果、初回の表示だけ先頭項目のプレビューが出て、2回目以降は（ホバーするか選択を動かすまで）出ない。
  実機確認で `popupCount` が初回2・2回目以降1になることで確認済み。表示のたびに
  プレビューを出し直すか、「選択項目のプレビューは出さない」に統一するかの方針決めが要る。
